PORT=5173
# If you deploy to a domain, add it here (comma-separated). Keep localhost for dev.
APP_ORIGINS=http://localhost:5173,http://theflysScoop.com
JWT_SECRET=change-me
# Auto-run AI scoop finder every N minutes (set 0 to disable)
AI_AUTO_RUN_INTERVAL_MINUTES=60
# Optional: if ffmpeg isn't on PATH, set this:
# FFMPEG_PATH=C:\ffmpeg\bin\ffmpeg.exe

# --- Manual verification payment links ---
CASHAPP_LINK="https://cash.app/$teon27"
VENMO_LINK=

# Admin token for approving/denying verification requests (send as x-admin-token header)
ADMIN_TOKEN=

# Dev-only toggles (keep off in production)
ALLOW_SELF_MEMBERSHIP_UPGRADE=0
ALLOW_DEV_VERIFICATION=0


